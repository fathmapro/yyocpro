#include<stdio.h>
int main()
{
int totDays,mon,remDays;
printf("enter the total no of days:");
scanf("%d",&totDays);
mon=totDays/30;
printf("%d days=%d months and %d days/n",totDays,mon,
remDays);
return 0;
}
